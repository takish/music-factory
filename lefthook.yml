pre-commit:
  parallel: true
  commands:
    type-check:
      glob: "*.{ts,tsx}"
      run: bun tsc --noEmit
    lint:
      glob: "*.{ts,tsx}"
      run: bun run lint:fix
    format:
      glob: "*.{ts,tsx}"
      run: bun run format:fix
